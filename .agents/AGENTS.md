# Workspace Rules

- Whenever you change the code for the Windows app (`desktop-app/`) or Android app (`android-app/`), you must ALWAYS:
  1. Commit the changes using git.
  2. Push the changes to the remote repository.
  3. Inform the user whether the change constitutes a major, minor, or patch update.
